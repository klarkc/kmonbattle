{
  "name": "kmonbattle",
  "version": "1.0.0",
  "description": "Pokemon battle game for browsers",
  "main": "src/loader.js",
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-core": "^6.22.1",
    "babel-plugin-transform-es2015-modules-amd": "^6.22.0",
    "babel-preset-env": "^1.1.8",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "copy": "mkdir -p build && cp index.html build && cp -r bower_components build",
    "build": "npm run copy && babel src -d build/src && ",
    "watch": "npm run copy && babel src -d build/src -w",
    "start": "npm run watch & http-server build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/klarkc/kmonbattle.git"
  },
  "keywords": [
    "pokemon",
    "battle",
    "browser",
    "game"
  ],
  "author": "klarkc",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/klarkc/kmonbattle/issues"
  },
  "homepage": "https://github.com/klarkc/kmonbattle#readme",
  "dependencies": {}
}
